# Shubham Agrawal Resume

This is my digital resume built using Streamlit.

## How to Run

1. Clone the repository
2. Install dependencies: `pip install -r requirements.txt`
3. Run the Streamlit app: `streamlit run app.py`
